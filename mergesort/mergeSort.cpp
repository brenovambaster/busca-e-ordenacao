#include <iostream>
#include <stdio.h>  /* printf, scanf, puts, NULL */
#include <stdlib.h> /* srand, rand */
#include <time.h>
#define TAM_MAX_VET 100
using namespace std;

int lista[TAM_MAX_VET];

int mergeSort(int *lista, int inicio = 0, int fim = NULL)
{
    if (fim == NULL)
    {
        fim = sizeof(lista);
    }
    return 0;
}

int main()
{
    mergeSort(lista, 1, 1);
}